#include "main.ih"

int main()
{
    Calculator beepboop;
    beepboop.run();
    /*Symtab ST;
    string x = "quit";
    ST.find(x);
    ST.find("lalala");
    
    Tokenizer token;
    token.nextToken();
    
    cout << token.ident() << " " << token.charValue() 
        << " " << token.intValue() << " " << token.doubleValue() 
        << " " << token.token() << '\n';
    */
    //Value temp;
    //Value temp2(3);
    
    //ST.at(ST.findIdx(x)).assign();
    
    //cout << ST.find(x).type() << '\n';
    //cout << ST.at(ST.findIdx(x)).type() << '\n';
    //cout << ST.at(ST.findIdx(x)).value().charValue() << '\n';
}
