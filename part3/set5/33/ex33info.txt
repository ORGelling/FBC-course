Exercise 33: Define a concept to verify that certain math operations are 
             available

When we compile this with the strings present we get an error telling us:

main.cc:16:17: error: no matching function for call to (our function name)

continuing with:

math/math.h:7:6: note: template argument deduction/substitution failed:
math/math.h:7:6: note: constraints not satisfied

It then shows how all the different requirements of the concept are or are not
fulfilled.

The constraint prevents the compiler from instantiating this template for
strings, as those are not able to satisfy all of the concept's requirements.
