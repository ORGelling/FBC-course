#include "checkmath.h"
#include <string>

using namespace std;

int main()
{
    int one = 8;
    int two = 9;
    
    checkMath1(one, two);
    
    // string str1{ "Hello " };
    // string str2{ "World!" };
    
    //checkMath1(str1, str2);   // fails!
    
    //checkMath2(str1, str2);   // also fails, needs custom type
}
