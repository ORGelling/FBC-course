%filenames scanner
%interactive

%token-path = "tokens.h"

digits      [0-9]+

%%

[ \t\n]+                                // ignore

{digits}                                return Tokens::NR;

[[:alpha:]_][[:alnum:]_]*               return Tokens::VAR;

\n|.                                    return matched().front();
