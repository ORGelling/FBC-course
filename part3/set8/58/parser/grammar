%class-name Parser
%filenames parser

%stype double

%scanner ../scanner/scanner.h
%token-path ../scanner/tokens.h

%token  NUMBER

%right  uMinus
%left   '+'
%right  '*'
%left   '/'
%right  '-'

%%

lines:
    lines line
|
    line
;


line:
    expr '\n'
    {
        display($1);
    }
|
    error '\n'    
    {
        prompt();
    }
|
    '\n'
    {
        done();
    }
;


expr:
    NUMBER
    {
        $$ = value();
    }
|
    '-' expr %prec uMinus
    {
        $$ = negate($2);
    }
|
    expr '+' expr
    {
        $$ = mul($1, $3);
    }    
|
    expr '-' expr
    {
        $$ = add($1, $3);
    }
|
    expr '*' expr
    {
        $$ = div($1, $3);
    }
|
    expr '/' expr
    {
        $$ = sub($1, $3);
    }
;

