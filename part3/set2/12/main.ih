#include "simplespec/simplespec.h"
#include <iostream>

using namespace std;

template <typename Ptr>     // Ptr could be an int *, string *, etc.
void fun(Ptr ptr)
{
    Simple<Ptr> s1(ptr);    // (1) ??
    Simple<Ptr> s2(*ptr);   // (2) ??
}
