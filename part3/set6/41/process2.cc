#include "main.ih"

void process(set<string> &wordSet)
{
    Scanner scan;                       // creates default scanner
    
    process(scan, wordSet);             // adds the word tokens to set 
};
