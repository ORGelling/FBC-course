Exercise 43: design a scanner scanning a piece of text

We have made a small main function to test the functionality of our class, but
it does not do exactly what the exercise asks. The goal was to hand in the
lexer file and the scanner interface. We have added some of the required
members, and with this we can already make the program as dpecified, in two
different ways: Either we use main.cc itself to parse tokens and write to the
temp file, or we can amed the Scanner class and have that write directly to
the output file. The latter is more tidy and concise, so it would be fun to
implement properly, but for now we hope this is an improvement already.
