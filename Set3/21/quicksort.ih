#include <iostream>
#include <ctype.h>
#include <utility>

using namespace std;

size_t partition
(
    string strArray[], 
    size_t const left, 
    size_t const right
);
bool mustSwap(string &pivot, string &comparison);
string toLower(string &input);
