#include "shrink.ih"

    // by 

// static
void Shrink::moveSwap2(vector<string> &vect)
{
    vector<string> tmp(
        make_move_iterator(vect.begin()), 
        make_move_iterator(vect.end())
    );
    
    tmp.swap(vect);
}
