#include "workforce.ih"

    // by 

void WorkForce::workFloor(istream &in, ostream &out)
{
    thread administrator{ &WorkForce::admin, ref(out) };
    thread worker1{ &WorkForce::worker };
    thread worker1{ &WorkForce::worker };
    
    // input lines
    getWork(in);
    
    
}
